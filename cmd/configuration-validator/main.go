package main

import (
	"fmt"
	// aggregationv1 "github.com/envoyproxy/xds-relay/pkg/api/aggregation/v1"
)

func main() {
	// Load yaml string from flags

	// if err != nil, write error to stdout (stderr?) and error out
	fmt.Println("wow")
}
